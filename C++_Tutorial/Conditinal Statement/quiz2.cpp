#include<iostream>
using namespace std;

int main()
{
    while(1)
    {
        cout<<"Hii"<<endl;
        break;
    }

    // This is called as infinite loop because condition, it is always true.loops never stops even break statement isnide to stop the loop
     return 0;
}